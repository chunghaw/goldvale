/**
 * Stable identifiers for the seeded demo record, shared by the seed script and the
 * live query layer so `/pets/[id]` resolves to the same pet the seed wrote.
 */
export const DEMO_OWNER_EMAIL = "demo@goldvale.app";
export const OSCAR_PET_ID = "a0c5ca9e-0000-4000-8000-000000000001";
